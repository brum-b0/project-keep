using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class koblows : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }//comment made by cobos

    // Update is called once per frame
    void Update()
    {
        
    }
}
